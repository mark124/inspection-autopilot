[project]
name = "inspection-autopilot"
version = "0.1.0"
description = "Human-in-the-loop triage agent for county food-inspection results, powered by Qwen"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
